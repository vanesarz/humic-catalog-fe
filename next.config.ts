import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images-residence.summarecon.com", "bandungoke.com", "i.pinimg.com", "placehold.co"],
  },
};

export default nextConfig;
